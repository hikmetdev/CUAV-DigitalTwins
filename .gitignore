# Ortam / gizli anahtarlar
config/.env
.env
.claude/

# Python
__pycache__/
*.pyc

# Loglar
logs/

# Uçuş başına sahne özeti veritabanları (scene_db.py üretir)
database/

# Üretilen taktik raporlar (PDF/metin) — çıktı, kaynak değil.
reports/

# Model ağırlıkları — büyük binary dosyalar, git'e girmemeli.
# Bunlar proje kökünde kalmak zorunda: yolo.py modeli göreli yolla yükler ve
# ultralytics MobileCLIP'i çalışma dizininden okur (app.py cwd'yi BASE_DIR'e sabitler).
yoloe-26s-seg.pt
mobileclip2_b.ts
*.pt
*.engine

# Eğitim çıktıları
runs/

# Veriseti içeriği (iskelet + data.yaml korunur)
dataset/images/
dataset/labels/

# Yerel video kaynağı (drone.mp4) — büyük medya, depoya girmez.
drone.mp4
